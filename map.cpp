#include<iostream>
#include<unordered_map>
using namespace std;
/*int main()
{
	unordered_multimap<char, int> m;
	m.insert(pair<char,int>('a', 24));
	m.insert(pair<char,int>('z', 44));
	m.insert(pair<char,int>('x',11));
	m.insert(pair<char,int>('a', 20));
	unordered_multimap<char, int>::iterator it;
	it=m.begin();
	while(it!=m.end())
	{
		cout<<"  "<<it->first<<"    "<<it->second;
		it++;
	}



}*/
