#include<iostream>
#include<vector>
//#include<iterator>
using namespace std;

/*int main()
{
vector<int> v2(5);
int *p= v2.data();
*p=10;
++p;
*p=23;
p[3]=1;
vector<int>::iterator it3= v2.begin();
// auto it5=v2.emplace(it3,444);
	//	 cout<<"q values is"<<*it5<<endl;
	for(;it3!=v2.end(); it3++)
		cout<<"iterator values  : "<<*it3;
	//int a[]={23,43,43,232,232};
	//cout<<"size of array        "<<sizeof(a)/sizeof(int)<<endl;;
	vector<int> v;
	cout<<"vector size:            "<<v.size()<<endl;
	v.push_back(4);
	v.push_back(5);
	v.push_back(10);
	v.push_back(6);
	v.push_back(7);
	v.push_back(2);

	cout<<"v CAPACITY equal    "<< v.capacity()<<endl;
	cout<<"v max CAPACITy equal    "<< v.max_size()<<endl;
	vector<int>::reverse_iterator it= v.rbegin();
	for(;it!=v.rend(); it++)
		cout<<"iterator values : "<<*it;
	cout<<"end vector size:            "<<v.size()<<endl;

	v.push_back(34);
	v.push_back(56);
	vector<int> v1;
	it= v.rbegin();
	v1.assign(it, v.rend());
	cout<<"v1 CAPACITY equal     "<< v1.capacity()<<endl;
	cout<<"v1 max CAPACITy equal     "<< v1.max_size()<<endl;
	cout<<"end vector v1    size:            "<<v1.size()<<endl;
	for(int i=0;i<v1.size();i++)
	{
		cout<<"vector values      "<<v1.at(i)<<endl;

	}

	return 0;
}*/
